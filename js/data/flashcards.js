// ── True flashcards: self-graded recall with SM-2 lite scheduling ─────────
// Show front. User thinks. Click "Reveal." Self-grade with 4 buttons.
// Schedule persists via localStorage; stats logged via /api/log-quiz.
const FLASHCARDS = [
  { id: 'fc_twosum_complexity', category: 'pattern', level: 'easy',
    front: 'What\'s the optimal time complexity for Two Sum, and what data structure achieves it?',
    back: 'O(n) with a hash map (value → index)',
    detail: 'Brute force pairs every (i, j) → O(n²). The hash map gives O(1) lookup of "have I seen the complement?" → one pass.' },
  { id: 'fc_sliding_window_signal', category: 'pattern', level: 'easy',
    front: 'You see "longest contiguous substring/subarray meeting some condition." What pattern?',
    back: 'Sliding window',
    detail: 'Expand R, shrink L when the invariant breaks. Track best window length. Used in: longest no-repeat, min window substring, longest replacement.' },
  { id: 'fc_two_pointers_signal', category: 'pattern', level: 'easy',
    front: 'When does the converging-two-pointers pattern apply?',
    back: 'Sorted array + need-to-find-pair OR palindrome-check OR partition problems',
    detail: 'Sorted-ness is the enabler — moving the smaller pointer right gives a bigger value, so each step eliminates a candidate.' },
  { id: 'fc_bs_signal', category: 'pattern', level: 'easy',
    front: 'When can you use binary search?',
    back: 'Sorted input OR a monotonic predicate (smallest x such that f(x) is true)',
    detail: 'The hidden form: binary searching on the ANSWER itself. Example: "smallest capacity ship to deliver within D days." Sorted-ness comes from monotonicity, not from the array.' },
  { id: 'fc_dfs_vs_bfs', category: 'pattern', level: 'easy',
    front: 'BFS vs DFS — which one for shortest path in an unweighted graph?',
    back: 'BFS',
    detail: 'BFS visits nodes level-by-level → first time you reach the target is the shortest distance. DFS could find a longer path first.' },
  { id: 'fc_topological_sort', category: 'pattern', level: 'medium',
    front: 'A scheduling problem where some tasks depend on others. What pattern?',
    back: 'Topological sort (Kahn\'s algorithm or DFS-based)',
    detail: 'Kahn\'s: start with in-degree-0 nodes, remove them, decrement neighbors\' in-degree, repeat. If you can\'t process all nodes, there\'s a cycle.' },
  { id: 'fc_heap_top_k', category: 'pattern', level: 'medium',
    front: 'You need the top K largest items from a stream of n items. What\'s the optimal approach?',
    back: 'Min-heap of size K → O(n log K)',
    detail: 'Push every item; pop when heap exceeds K. Smaller heap-size beats sorting (O(n log n)) when K is small. Counter-intuitive: MIN-heap for top-K largest.' },
  { id: 'fc_dp_signal', category: 'pattern', level: 'medium',
    front: 'When does dynamic programming apply?',
    back: 'Overlapping subproblems + optimal substructure',
    detail: 'Overlapping = the same subproblem appears many times (cache it). Optimal substructure = the optimal answer is built from optimal sub-answers.' },
  { id: 'fc_backtracking_signal', category: 'pattern', level: 'medium',
    front: 'When does backtracking apply?',
    back: 'Combinatorial search: subsets, permutations, N-Queens, partitioning, generating valid configurations',
    detail: 'Choose → recurse → un-choose. Without the "un-choose" step (pop), siblings inherit state they shouldn\'t.' },
  { id: 'fc_kadane', category: 'pattern', level: 'medium',
    front: 'What\'s Kadane\'s algorithm and what does it solve?',
    back: 'Maximum Subarray. cur = max(x, cur + x); best = max(best, cur).',
    detail: 'A negative prefix can never help — drop it and start fresh at x. That insight makes it O(n) instead of O(n²) or O(n³).' },
  { id: 'fc_floyd', category: 'pattern', level: 'medium',
    front: 'What\'s Floyd\'s tortoise and hare used for?',
    back: 'Cycle detection in a linked list with O(1) extra space',
    detail: 'Slow advances 1, fast advances 2. If there\'s a cycle, they meet inside it. Hash-set works too but uses O(n) space.' },
  { id: 'fc_invariant_sw', category: 'concept', level: 'medium',
    front: 'In a sliding window, what does "invariant" mean and why does it matter?',
    back: 'A property the current window must always satisfy. You expand until it\'s violated, then shrink until it holds again.',
    detail: 'Examples: "no duplicate chars," "sum ≤ K," "≤ 2 distinct chars." The whole loop is a dance between R (expand) and L (shrink) maintaining the invariant.' },
  { id: 'fc_amortized', category: 'concept', level: 'medium',
    front: 'What does "amortized O(1)" mean?',
    back: 'Average cost per operation across a sequence is O(1), even if individual ops are sometimes expensive.',
    detail: 'Classic example: dynamic array push is O(1) amortized, but the resize-and-copy step is O(n). Spread across n pushes, the total is O(n) → O(1) average.' },
  { id: 'fc_stable_sort', category: 'concept', level: 'easy',
    front: 'What does it mean for a sort to be "stable"?',
    back: 'Equal-key elements keep their original relative order',
    detail: 'Merge sort is stable. Quick sort is not (in standard implementations). Matters when sorting by multiple keys.' },
  { id: 'fc_in_place', category: 'concept', level: 'easy',
    front: 'What does "in-place" mean for an algorithm?',
    back: 'Uses O(1) extra space (or close to it) — modifies the input rather than allocating a new structure',
    detail: 'Quick sort is in-place. Merge sort is not (needs an auxiliary array). In-place trades space for cache-friendliness.' },
  { id: 'fc_dummy_head', category: 'concept', level: 'easy',
    front: 'What\'s the "dummy head" trick in linked lists, and why use it?',
    back: 'A placeholder node before the real head, so insertion/deletion at the front has the same shape as everywhere else',
    detail: 'Without it, you need special-case branches for "what if I\'m removing the head?" The dummy unifies the code.' },
  { id: 'fc_recursion_template', category: 'concept', level: 'easy',
    front: 'What\'s the universal recursion template for tree problems?',
    back: 'Handle null base case → recurse on children → combine the results',
    detail: 'Almost every tree problem fits: max-depth, sum, balanced, mirror, LCA. The "combine" step is what changes per problem.' },
  { id: 'fc_dp_top_vs_bottom', category: 'concept', level: 'medium',
    front: 'Top-down DP vs bottom-up DP — what\'s the difference?',
    back: 'Top-down = recursion + memo cache. Bottom-up = iterative loop filling a table from base cases up.',
    detail: 'Top-down is closer to the natural problem statement. Bottom-up usually has better constants (no recursion overhead) and can sometimes optimize space.' },
  { id: 'fc_overflow_mid', category: 'gotcha', level: 'medium',
    front: 'In binary search, why use <code>lo + (hi - lo) // 2</code> instead of <code>(lo + hi) // 2</code>?',
    back: 'To avoid integer overflow when lo + hi exceeds INT_MAX',
    detail: 'Python doesn\'t overflow but use the safe form anyway — it\'s a portable habit. In Java/C++ this is a real bug.' },
  { id: 'fc_path_snapshot', category: 'gotcha', level: 'medium',
    front: 'In backtracking, why <code>result.append(path[:])</code> instead of <code>result.append(path)</code>?',
    back: 'path is mutated by later recursion — without the snapshot, all stored results point to the same (eventually empty) list',
    detail: '<code>path[:]</code> is a shallow copy. Other valid forms: <code>list(path)</code>, <code>tuple(path)</code>.' },
  { id: 'fc_check_then_store', category: 'gotcha', level: 'medium',
    front: 'In Two Sum, why CHECK for the complement before storing the current value?',
    back: 'Otherwise an element with 2*n == target would falsely "find" itself',
    detail: 'Order matters: check first (does seen contain complement?), then store. Always.' },
  { id: 'fc_visited_in_dfs', category: 'gotcha', level: 'medium',
    front: 'In graph DFS/BFS, what happens if you forget to track visited nodes?',
    back: 'Infinite loop on graphs with cycles',
    detail: 'Trees can\'t have cycles so they\'re fine. Anything with cycles needs a visited set.' },
  { id: 'fc_python_default_arg', category: 'gotcha', level: 'medium',
    front: 'In Python, why is <code>def f(memo={})</code> as a recursion cache risky?',
    back: 'The dict is created ONCE and persists across all calls — pollution between separate top-level invocations',
    detail: 'Safer: <code>def f(n, memo=None): memo = memo or {}</code>, or use <code>@functools.lru_cache</code>.' },
  { id: 'fc_negative_dp_init', category: 'gotcha', level: 'medium',
    front: 'In Coin Change, what sentinel do you initialize <code>dp[]</code> with, and why not 0?',
    back: '<code>float(\'inf\')</code> (or a number larger than any valid answer)',
    detail: '0 would make every subproblem look "solvable with zero coins." We need a value that always loses the <code>min()</code> until a real solution is found.' },
  { id: 'fc_two_choices_2n', category: 'concept', level: 'medium',
    front: 'For each element you choose include OR exclude, recursing on the rest. How many subsets total?',
    back: '2ⁿ — every element is a binary choice',
    detail: 'This is why subsets, power-set, and similar problems are O(n · 2ⁿ): 2ⁿ subsets, each O(n) to copy.' },
  { id: 'fc_bfs_level_size', category: 'concept', level: 'medium',
    front: 'How do you process BFS one LEVEL at a time (not just one node at a time)?',
    back: 'Snapshot <code>len(queue)</code> at the start of each iteration; process exactly that many nodes',
    detail: 'Without this, level boundaries get smeared as you enqueue children inside the loop. The pattern: <code>for _ in range(len(q)): node = q.popleft(); ...</code>' },
];

const FLASHCARDS_PER_SESSION = 12;
