// ── Optimal solutions for each problem ─────────────────────────────────────
const SOLUTIONS = {
  // Array
  1: { approach: 'Hash map of value→index in one pass. For each element, check if (target − x) is already in the map.', time: 'O(n)', space: 'O(n)', insight: 'Trade space for time — replaces O(n²) brute force.' },
  2: { approach: 'Track the minimum price seen so far; the answer is max(price − minSoFar) over the scan.', time: 'O(n)', space: 'O(1)', insight: 'You only need the smallest valid buy day to compute every sell day.' },
  3: { approach: 'Walk the array adding to a hash set; return true on the first duplicate insertion.', time: 'O(n)', space: 'O(n)', insight: 'Sorting also works for O(n log n) / O(1).' },
  4: { approach: 'Two passes: prefix products from the left, then multiply by suffix products from the right.', time: 'O(n)', space: 'O(1) excluding output', insight: 'No division allowed — the prefix/suffix trick replaces it.' },
  5: { approach: 'Kadane: keep a running sum; reset to current element if running sum goes negative; track max.', time: 'O(n)', space: 'O(1)', insight: 'A negative-prefix can never help a future subarray — drop it.' },
  6: { approach: 'Track running max AND min — a negative element flips them. Update both on each step.', time: 'O(n)', space: 'O(1)', insight: 'Negative numbers turn a min into a max — must carry both.' },
  7: { approach: 'Binary search; if arr[mid] > arr[hi] the min is in (mid, hi], else in [lo, mid].', time: 'O(log n)', space: 'O(1)' },
  8: { approach: 'Binary search; check which half is sorted, then check if target is in it; recurse on that half.', time: 'O(log n)', space: 'O(1)', insight: 'One half is always sorted — exploit that.' },
  9: { approach: 'Sort, then for each i do two-pointer search for −nums[i] in the rest; skip duplicates carefully.', time: 'O(n²)', space: 'O(1)' },
  10: { approach: 'Two pointers from both ends; area = min(h[L], h[R])·(R−L); move the shorter side inward.', time: 'O(n)', space: 'O(1)', insight: 'Moving the taller side can only shrink area — always move the shorter.' },
  // Binary
  11: { approach: 'a^b is sum without carry, (a&b)<<1 is the carry. Loop until carry = 0.', time: 'O(1)', space: 'O(1)' },
  12: { approach: 'Repeatedly do n &= (n − 1); count iterations until n = 0.', time: 'O(log n)', space: 'O(1)', insight: 'n & (n−1) clears the lowest set bit.' },
  13: { approach: 'dp[i] = dp[i >> 1] + (i & 1) — half the bits plus whether the LSB is set.', time: 'O(n)', space: 'O(n)' },
  14: { approach: 'XOR every value [0..n] with every index — the missing one survives.', time: 'O(n)', space: 'O(1)', insight: 'a ^ a = 0, so duplicates cancel out.' },
  15: { approach: 'Loop 32 times: shift result left, OR with (n & 1), shift n right.', time: 'O(1)', space: 'O(1)' },
  // Dynamic Programming
  16: { approach: 'Fibonacci: dp[i] = dp[i−1] + dp[i−2]; just keep the last two values.', time: 'O(n)', space: 'O(1)' },
  17: { approach: 'dp[a] = min(dp[a − coin] + 1) for each coin; dp[0] = 0.', time: 'O(amount · coins)', space: 'O(amount)' },
  18: { approach: 'Patience sort: maintain a tails array; binary-search insert each num. Length of tails = LIS.', time: 'O(n log n)', space: 'O(n)', insight: 'O(n²) DP is also accepted but slower.' },
  19: { approach: '2D DP: dp[i][j] = dp[i−1][j−1] + 1 if match else max(dp[i−1][j], dp[i][j−1]).', time: 'O(m·n)', space: 'O(m·n)' },
  20: { approach: 'dp[i] = any(dp[j] && s[j..i] in dict) for j < i.', time: 'O(n²·k)', space: 'O(n)', insight: 'Trie can speed up the inner check.' },
  21: { approach: 'dp[t] = sum of dp[t − num] for num in nums (note: order matters — permutations, not combinations).', time: 'O(target · n)', space: 'O(target)' },
  22: { approach: 'dp[i] = max(dp[i−1], dp[i−2] + nums[i]). Two rolling vars.', time: 'O(n)', space: 'O(1)' },
  23: { approach: 'Run House Robber on nums[1:] and nums[:n−1] separately; take the max.', time: 'O(n)', space: 'O(1)', insight: 'Circular constraint: first and last can\'t both be picked.' },
  24: { approach: 'dp[i] depends on whether s[i] is valid alone and whether s[i−1..i] is in 10..26.', time: 'O(n)', space: 'O(1)' },
  25: { approach: 'dp[i][j] = dp[i−1][j] + dp[i][j−1]. Only need previous row → O(n) space.', time: 'O(m·n)', space: 'O(min(m,n))' },
  26: { approach: 'Greedy: track furthest reachable index; fail if i > maxReach.', time: 'O(n)', space: 'O(1)', insight: 'Greedy beats DP here.' },
  // Graph
  27: { approach: 'BFS or DFS from the start node; map original→clone to avoid revisiting.', time: 'O(V + E)', space: 'O(V)' },
  28: { approach: 'Topological sort via BFS (Kahn\'s) — track in-degrees, queue zero-indegree nodes.', time: 'O(V + E)', space: 'O(V + E)', insight: 'If you can topologically sort, no cycle exists.' },
  29: { approach: 'BFS/DFS from each ocean\'s edge cells inward; cells reachable from BOTH are the answer.', time: 'O(m·n)', space: 'O(m·n)' },
  30: { approach: 'For each unvisited \'1\', BFS/DFS to mark its connected component; count how many starts.', time: 'O(m·n)', space: 'O(m·n) recursion' },
  31: { approach: 'Hash set of nums; for each n with no n−1 in set, count consecutive forward.', time: 'O(n)', space: 'O(n)', insight: 'Skip non-starts to keep it linear.' },
  32: { approach: 'Build a char graph from adjacent word pairs; topologically sort the chars.', time: 'O(C)', space: 'O(1) (26-letter alphabet)' },
  33: { approach: 'Union-Find: n−1 edges + all unite without conflict ⇒ tree.', time: 'O(n + e·α(n))', space: 'O(n)' },
  34: { approach: 'Union-Find on edges; count distinct roots at the end.', time: 'O(n + e·α(n))', space: 'O(n)' },
  // Interval
  35: { approach: 'Walk: copy intervals before, merge overlapping ones with the new interval, copy after.', time: 'O(n)', space: 'O(n)' },
  36: { approach: 'Sort by start; if next.start ≤ prev.end, merge by extending end; else push new.', time: 'O(n log n)', space: 'O(n)' },
  37: { approach: 'Sort by end; greedily keep earliest-finishing intervals; count those that conflict with last kept.', time: 'O(n log n)', space: 'O(1)', insight: 'Always keep the interval that frees up the soonest.' },
  38: { approach: 'Sort by start; check no overlap with the previous one.', time: 'O(n log n)', space: 'O(1)' },
  39: { approach: 'Sort starts and ends separately; sweep, track max active count. Or min-heap of end times.', time: 'O(n log n)', space: 'O(n)' },
  // Linked List
  40: { approach: 'Iterate with prev = null; flip each node\'s next pointer.', time: 'O(n)', space: 'O(1)' },
  41: { approach: 'Floyd\'s tortoise-and-hare: fast (2x) and slow (1x) pointers; meet ⇒ cycle.', time: 'O(n)', space: 'O(1)' },
  42: { approach: 'Walk both with a dummy head; attach the smaller node and advance.', time: 'O(n + m)', space: 'O(1)' },
  43: { approach: 'Min-heap of head nodes — pop smallest, push its next. Or pairwise divide-and-conquer.', time: 'O(N log k)', space: 'O(k)' },
  44: { approach: 'Two pointers: advance one by n, then move both until the first hits the end.', time: 'O(n)', space: 'O(1)' },
  45: { approach: 'Find middle (slow/fast), reverse second half, then interleave the two halves.', time: 'O(n)', space: 'O(1)' },
  // Matrix
  46: { approach: 'Use first row and first column as flag arrays; track the originals separately.', time: 'O(m·n)', space: 'O(1)' },
  47: { approach: 'Maintain top/bottom/left/right boundaries; walk one layer, shrink boundaries, repeat.', time: 'O(m·n)', space: 'O(1)' },
  48: { approach: 'Transpose the matrix (swap M[i][j] with M[j][i]), then reverse each row.', time: 'O(n²)', space: 'O(1)' },
  49: { approach: 'DFS from each cell, marking visited; backtrack on return. Stop early on match.', time: 'O(m·n·4^L)', space: 'O(L)' },
  // String
  50: { approach: 'Sliding window with a set of chars in window; on duplicate, advance left until removed.', time: 'O(n)', space: 'O(min(n, Σ))' },
  51: { approach: 'Sliding window where (window size − maxCharCount) ≤ k; shrink left when violated.', time: 'O(n)', space: 'O(1)', insight: 'You don\'t need to recompute maxCount every step — it only matters that it grew.' },
  52: { approach: 'Sliding window with target counter; expand right until valid, then contract left to minimize.', time: 'O(n)', space: 'O(Σ)' },
  53: { approach: 'Char-count arrays of length 26 — compare. Or sort and compare.', time: 'O(n)', space: 'O(1)' },
  54: { approach: 'Group by sorted-string key (or 26-tuple of counts) in a hash map.', time: 'O(n·k log k)', space: 'O(n·k)' },
  55: { approach: 'Stack — push opens; on close, pop and verify match.', time: 'O(n)', space: 'O(n)' },
  56: { approach: 'Two pointers from ends; skip non-alphanumerics; compare lowercase chars.', time: 'O(n)', space: 'O(1)' },
  57: { approach: 'Expand around each center (n centers + n−1 between-pairs). Track longest.', time: 'O(n²)', space: 'O(1)', insight: 'Manacher\'s gives O(n) but is rarely required.' },
  58: { approach: 'Same as #57 — count expansions instead of tracking the longest.', time: 'O(n²)', space: 'O(1)' },
  59: { approach: 'Encode each string as "length#string"; on decode, parse the length prefix.', time: 'O(n)', space: 'O(n)' },
  // Tree
  60: { approach: 'DFS: depth = 1 + max(depth(left), depth(right)).', time: 'O(n)', space: 'O(h) recursion' },
  61: { approach: 'Recurse: both null OR same value AND left == left AND right == right.', time: 'O(n)', space: 'O(h)' },
  62: { approach: 'Swap children at each node, recurse on both.', time: 'O(n)', space: 'O(h)' },
  63: { approach: 'DFS returning best one-sided gain (max(0, ...)); track global best as a "split through this node" value.', time: 'O(n)', space: 'O(h)', insight: 'Can\'t take both child paths upward — but you CAN combine them at the current node.' },
  64: { approach: 'BFS with queue; capture the size at each level to group children.', time: 'O(n)', space: 'O(n)' },
  65: { approach: 'Pre-order with null markers ("#"); deserialize via recursive read from a queue of tokens.', time: 'O(n)', space: 'O(n)' },
  66: { approach: 'For each node in main tree, check isSameTree against root of subtree.', time: 'O(m · n)', space: 'O(h)', insight: 'Tree-hashing or KMP-on-traversal gives O(n + m).' },
  67: { approach: 'pre[0] is the root; find it in inorder to split into left/right halves; recurse with index lookup.', time: 'O(n)', space: 'O(n)' },
  68: { approach: 'DFS with (lo, hi) bounds — left subtree must be < node < right subtree.', time: 'O(n)', space: 'O(h)', insight: 'Don\'t just compare to direct children — bounds propagate.' },
  69: { approach: 'In-order traversal; stop at the k-th visited node.', time: 'O(h + k)', space: 'O(h)' },
  70: { approach: 'Walk: if both p and q < node, go left; both > node, go right; else node is LCA.', time: 'O(h)', space: 'O(1)' },
  71: { approach: 'Tree of children dicts plus an isWord flag at terminal nodes.', time: 'O(L) per op', space: 'O(total chars)' },
  72: { approach: 'Trie + DFS where "." branches into all children.', time: 'O(L) avg, O(26^L) worst', space: 'O(W·L)' },
  73: { approach: 'Build a trie of words; DFS on grid following trie edges; remove found words to prune.', time: 'O(m·n·4^L)', space: 'O(W·L)' },
  // Heap
  74: { approach: 'Counter, then min-heap of size k. Or bucket sort by frequency for O(n).', time: 'O(n log k)', space: 'O(n)' },
  75: { approach: 'Max-heap for the lower half, min-heap for the upper half; balance sizes ±1; median from tops.', time: 'O(log n) addNum, O(1) findMedian', space: 'O(n)', insight: 'The two heaps are the trick — neither alone supports median.' },
  // Hard-tier expansion
  76: { approach: 'Two pointers from both ends; track max-left and max-right; trapped water at i = min(maxL, maxR) − height[i]. Move whichever side has the smaller max.', time: 'O(n)', space: 'O(1)', insight: 'O(n) DP arrays also work; two-pointer is O(1) space.' },
  77: { approach: 'Monotonic decreasing deque of indices. Pop front if out of window; pop back while back ≤ current; push current. Front holds the window max.', time: 'O(n)', space: 'O(k)', insight: 'Heap-based is O(n log k); monotonic deque is strictly better.' },
  78: { approach: 'Hash map of key → DLL node, plus a doubly linked list for LRU order. get: move node to head. put: insert at head, evict tail if over capacity.', time: 'O(1) per op', space: 'O(capacity)', insight: 'Hash map alone is O(1) lookup but no order; DLL alone has order but O(n) lookup. Combined gives both.' },
  79: { approach: 'Monotonic increasing stack of bar indices. When a shorter bar arrives, pop and compute the rectangle that the popped bar dominates: height = popped, width = i − stack-top − 1.', time: 'O(n)', space: 'O(n)', insight: 'Each bar pushed and popped at most once.' },
  80: { approach: '2D DP: dp[t][i] = max profit using ≤ t transactions through day i. dp[t][i] = max(dp[t][i−1], price[i] − price[j] + dp[t−1][j−1]) maintained via running max.', time: 'O(n·k)', space: 'O(n·k)', insight: 'When k ≥ n/2, problem reduces to unlimited transactions (sum positive deltas).' },
  81: { approach: 'dp[i][j] = edit distance between s1[:i] and s2[:j]. If chars match: dp[i−1][j−1]. Else: 1 + min(insert, delete, replace).', time: 'O(m·n)', space: 'O(m·n)', insight: 'Can space-optimize to O(min(m, n)) by rolling rows.' },
  82: { approach: '2D DP: dp[i][j] = does s[:i] match p[:j]. Handle "*" by trying both 0 occurrences (dp[i][j−2]) and "match this char" (dp[i−1][j] if char/dot matches).', time: 'O(m·n)', space: 'O(m·n)', insight: 'The "*" handling has two sub-cases — easy to miss.' },
  83: { approach: 'Interval DP: dp[i][j] = max coins from bursting all balloons in (i, j) exclusive. The LAST balloon burst in (i, j) is the key — try each k as the last.', time: 'O(n³)', space: 'O(n²)', insight: '"Last to burst" framing is what makes the DP work — first-to-burst doesn\'t.' },
  84: { approach: 'State-machine DP. States: held (own stock), sold (just sold, in cooldown), rest (no stock, no cooldown). Track running max for each.', time: 'O(n)', space: 'O(1)', insight: 'Cooldown breaks pure greedy — explicit states are required.' },
  85: { approach: 'Max-heap of size k keyed by negative distance. Push every point; pop if heap exceeds k. The k remaining are the closest.', time: 'O(n log k)', space: 'O(k)', insight: 'Quickselect gives O(n) average but the heap is more idiomatic.' },
  86: { approach: 'Stack of indices. On "(", push. On ")", pop; if stack empty, push current as new "base." Length of valid substring ending at i = i − top.', time: 'O(n)', space: 'O(n)', insight: 'Alternatively: 2D DP with dp[i] = length ending at i, with a recurrence on prior matched parens.' },
  87: { approach: 'Reverse k nodes in place using the standard 3-pointer flip; then connect to the next group recursively. Pre-check that k nodes are available.', time: 'O(n)', space: 'O(1) iterative · O(n/k) recursive', insight: 'The "check k nodes available" step prevents reversing partial groups.' },
  88: { approach: 'Binary search the partition split on the smaller array. For partition (i, j) where i + j = (m+n+1)/2, check left-max ≤ right-min on both arrays.', time: 'O(log(min(m, n)))', space: 'O(1)', insight: 'Merging is O(m+n). Binary search on partitions is the only way to hit log-time.' },
  89: { approach: 'Backtracking. For each empty cell, try 1-9; check row/col/box; recurse; un-fill on failure. Prune by tracking which digits are already used per row/col/box.', time: 'O(9^(empty cells))', space: 'O(1)', insight: 'Standard backtracking; aggressive pruning makes it fast in practice.' },
  90: { approach: 'Backtracking row-by-row. Track occupied columns and the two diagonals (col + row, col − row). For each row, try every safe column; recurse; un-set.', time: 'O(n!)', space: 'O(n)', insight: 'The "two diagonal sets" trick beats checking against placed queens directly.' },
};
